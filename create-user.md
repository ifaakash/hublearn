# Setup the user

uname -a - system information
uname -m - get to konw the architecture

useradd -m <user-name> [ -m create the user home directory ]

userdel -r <user-name> - delete the user + home directory

id <user-name> - provide the user + group information for the user

cat /etc/passwd
